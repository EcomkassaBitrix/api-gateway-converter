# Порт сервера
PORT=3001

# База данных PostgreSQL (опционально, для логирования)
# DATABASE_URL=postgresql://user:password@localhost:5432/ekomkassa_gateway

# Окружение (development / production)
NODE_ENV=development
